#include<stdio.h>
int prime(int n);
int main()
{
	int n,i,count=0;
	scanf("%d",&n);
	int res=prime(n);
	if(res==1)
	{
		printf("prime");
	}
    else
        printf("not");
}
int prime(int n)
{
	int i,count=0;
	for(i=1;i<=n;i++)
	{
	   if(n%i==0)
	   {
	   	count+=1;
		}	
	}
	if(count==2)
	  return 1;
	else
	  return 0;
}
