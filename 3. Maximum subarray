#include<stdio.h>
int main()
{
    int a[]={5,4,-1,7,8};
    int sum=0,i;
    int max=0;
    int n=sizeof(a)/sizeof(a[0]);
    for(i=0;i<n;i++)
    {
        int temp=sum;
        sum=a[i]+a[i+1];
        if(sum>temp){
            max=sum;
        }
    }
    printf("%d",max);
}
