#include<stdio.h>
int fact(int n);
int main()
{
	int n;
	scanf("%d",&n);
	printf("factorial of %d is %d",n,fact(n));
}
int fact(int n)
{
	int f;
	if(n<=1)
	return n;
	else
	f=n*fact(n-1);
	return f;
}
