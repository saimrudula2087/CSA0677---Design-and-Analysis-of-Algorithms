#include<stdio.h>
int gcd(int n1,int n2);
int main()
{
	int n1,n2,res;
	scanf("%d%d",&n1,&n2);
	res=gcd(n1,n2);
	printf("gcd = %d",res);
}
int gcd(int a, int b)
{
	int i;
	for(i=1;i<=a&&i<=b;i++)
	{
		if(a%i==0&&b%i==0)
		  return i;
	}
}
