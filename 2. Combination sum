#include<stdio.h>
int main()
{
	int a[]={2,3,6,7};
	int target=7;
	int x=2;
	int n=sizeof(a)/sizeof(a[0]);
	for(int i=0;i<n;i++)
	{
		if(x+a[i]+a[i+1]==target)
		{
			printf("%d %d %d",x,a[i],a[i+1]);
		}
	}
}
